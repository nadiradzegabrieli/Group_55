// 1 codewars

function replace(s) {
  let result = "";
  let vowels = "aeiouAEIOU"
      if(vowels.includes(s[i])){
        result += "!";
      }else{
        result += s[i];
      }
  return result
}

console.log(replace)

// 2 Codewars

function sumTwoSmallestNumbers(numbers) {  
numbers.sort((a, b) => a-b)
let sum = numbers[a + b]
return SumTwoSmallestNumbers
}

// 3 Codewars

var isSquare = function(n) {
  return n >= 0 && Number.isInteger(Math.sqrt(n));
};


